---
- name: Actualisation pole emploi
  category: business
- task: aller marcher pendant 10 minutes
  category: sport
- task: manger cinq fruits et legumes
  category: health
- task: participer a un atelier
  category: hobby


  <% YAML.load_file('data/tasks.yml').each do |task| %>
    <hr>

    <div data-controller="enfant" data-enfant-category-value="<%= question["category"] %>" data-action="yes->parent#addCategory no->parent#next">
      <%= question["name"] %> - <%= question["category"] %>

      <span data-action="click->enfant#yes">Oui</span>

      <span data-action="click->enfant#no">Non</span>
    </div>
  <% end %>
